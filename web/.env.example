# Frontend config
# API base URL for the Flask service
VITE_API_BASE_URL=http://43.134.90.115:8001

# Public base path for deploying under a sub-directory.
# Example: deploy under http://example.com/wechat/ then set:
# VITE_PUBLIC_BASE=/wechat/
VITE_PUBLIC_BASE=/

# Production example (option B):
# Deploy under /wechat and call backend directly:
# VITE_PUBLIC_BASE=/wechat/
# VITE_API_BASE_URL=http://43.134.90.115:8001

# Dev-only: force using same-origin + Vite proxy (/api -> target) to avoid CORS.
# Default is enabled in dev; set to 0 to disable.
VITE_FORCE_PROXY=1

# Dev-only (optional): Vite proxy target for /api.
# If set, you can leave VITE_API_BASE_URL empty and rely on same-origin + proxy.
# VITE_API_PROXY_TARGET=http://43.134.90.115:8001
